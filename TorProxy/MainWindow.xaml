<Window x:Class="Tor_Proxy.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tor Proxy (MoGlitch)" Height="420" Width="480"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True" WindowStyle="None" Background="Transparent"
        Closing="Window_Closing">

    <Window.Resources>
        <!-- Icons -->
        <Geometry x:Key="IconUpload">M9,16V10H5L12,3L19,10H15V16H9M5,20V18H19V20H5Z</Geometry>
        <Geometry x:Key="IconHash">M14,12H18V10H14V12M14,16H18V14H14V16M10,12H14V10H10V12M10,16H14V14H10V16M6,12H10V10H6V12M6,16H10V14H6V16M4,20H20V8H4V20M4,6H20V4H4V6Z</Geometry>
        <Geometry x:Key="IconRocket">M12,2C12,2 11,3 10,5C9,7 7,11 7,13A5,5 0 0,0 12,18A5,5 0 0,0 17,13C17,11 15,7 14,5C13,3 12,2 12,2Z</Geometry>
        <Geometry x:Key="IconFolder">M10,4H4C2.89,4 2,4.89 2,6V18A2,2 0 0,0 4,20H20A2,2 0 0,0 22,18V8C22,6.89 21.1,6 20,6H12L10,4Z</Geometry>

        <!-- TitleBar Buttons Style -->
        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#aaa"/>
            <Setter Property="Width" Value="35"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <TextBlock Text="{TemplateBinding Content}" VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="Segoe MDL2 Assets" FontSize="12"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#44FFFFFF"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsMouseOver" Value="True" />
                        <Condition Property="Content" Value="&#xE8BB;" />
                    </MultiTrigger.Conditions>
                    <Setter Property="Background" Value="#E81123"/>
                    <Setter Property="Foreground" Value="White"/>
                </MultiTrigger>
            </Style.Triggers>
        </Style>

        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="BorderBrush" Value="#88FFFFFF"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="35,8,8,8"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Path Data="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" Fill="#88FFFFFF" Stretch="Uniform" Width="20" Height="20" HorizontalAlignment="Left" Margin="5,0,0,0"/>
                            <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="True">
                                <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="true">
                                <Setter Property="BorderBrush" Value="#00BFFF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- This Grid is the single child of the Window and solves the error -->
    <Grid>
        <!-- Acrylic Blur Background -->
        <Border CornerRadius="10" Background="#AA222228">
            <Border.Effect>
                <BlurEffect Radius="20"/>
            </Border.Effect>
        </Border>

        <!-- Main Content Border -->
        <Border CornerRadius="10" BorderThickness="1" BorderBrush="#44FFFFFF">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="35"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Custom Title Bar -->
                <Grid Grid.Row="0" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="MinimizeButton" Content="&#xE921;" Style="{StaticResource TitleBarButtonStyle}" Click="MinimizeButton_Click"/>
                        <Button x:Name="CloseButton" Content="&#xE8BB;" Style="{StaticResource TitleBarButtonStyle}" Click="CloseButton_Click"/>
                    </StackPanel>
                    <TextBlock Text="Tor Proxy Gen" Foreground="#DDFFFFFF" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="15,0,0,0" FontSize="14"/>
                </Grid>

                <!-- Content Area -->
                <Grid Grid.Row="1">
                    <StackPanel Margin="30,20,30,20">
                        <TextBlock Text="مسار ملف التشغيل" Foreground="#aaa" FontSize="12" Margin="0,0,0,5" TextAlignment="Left"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBox x:Name="TorPathBox" Grid.Column="0" Style="{StaticResource ModernTextBox}" Tag="{StaticResource IconUpload}" 
                                     AllowDrop="True" PreviewDragOver="TorPathBox_PreviewDragOver" Drop="TorPathBox_Drop" 
                                     ToolTip="Drag &amp; drop tor.exe here or use the browse button"/>

                            <Button x:Name="BrowseButton" Grid.Column="1" Height="35" Width="40" Margin="5,0,0,2" VerticalAlignment="Center" Click="BrowseButton_Click">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="Cursor" Value="Hand"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="4" BorderThickness="1" BorderBrush="#88FFFFFF">
                                                        <Path Data="{StaticResource IconFolder}" Fill="#88FFFFFF" Stretch="Uniform" Width="20" Height="20" />
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#33FFFFFF"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Grid>

                        <TextBlock Text="عدد النسخ المطلوبة" Foreground="#aaa" FontSize="12" Margin="0,20,0,5" TextAlignment="Left"/>
                        <TextBox x:Name="ProxyCountBox" Text="1" Style="{StaticResource ModernTextBox}" Tag="{StaticResource IconHash}"/>

                        <StackPanel Orientation="Horizontal" Margin="0,30,0,15">
                            <Button x:Name="StartButton" Height="45" Width="200" Click="StartTorInstances_Click" Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="#FF007ACC"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="FontSize" Value="16"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                            <Path Data="{StaticResource IconRocket}" Fill="White" Width="20" Height="20" Margin="0,0,10,0" Stretch="Uniform"/>
                                                            <ContentPresenter/>
                                                        </StackPanel>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="border" Property="Background" Value="#FF0099FF"/>
                                                        </Trigger>
                                                        <Trigger Property="IsPressed" Value="True">
                                                            <Setter TargetName="border" Property="Background" Value="#FF005A9E"/>
                                                        </Trigger>
                                                        <Trigger Property="IsEnabled" Value="False">
                                                            <Setter Property="Opacity" Value="0.5"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                                <Button.Content>
                                    تشغيل
                                </Button.Content>
                            </Button>
                            <Button x:Name="StopButton" Height="45" Width="180" Margin="10,0,0,0" Click="StopButton_Click" IsEnabled="False" Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="#D32F2F"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="FontSize" Value="16"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="border" Property="Background" Value="#F44336"/>
                                                        </Trigger>
                                                        <Trigger Property="IsPressed" Value="True">
                                                            <Setter TargetName="border" Property="Background" Value="#B71C1C"/>
                                                        </Trigger>
                                                        <Trigger Property="IsEnabled" Value="False">
                                                            <Setter Property="Opacity" Value="0.5"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                                <Button.Content>
                                    إيقاف
                                </Button.Content>
                            </Button>
                        </StackPanel>

                        <!-- *** FIX: Replaced ListBox with a TextBox for copyable logs *** -->
                        <TextBox x:Name="LogBox" 
                                 Background="#33FFFFFF" 
                                 BorderThickness="0" 
                                 Foreground="White"
                                 Height="100"
                                 IsReadOnly="True"
                                 TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto"/>

                    </StackPanel>
                    <TextBlock Text="Developed by MoGlitch"
                               Foreground="#66FFFFFF" 
                               FontSize="11"
                               HorizontalAlignment="Right"
                               VerticalAlignment="Bottom"
                               Margin="0,0,15,10"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
